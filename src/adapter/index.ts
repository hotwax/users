import {
  api,
  client,
  getConfig,
  hasError,
  initialise,
  logout,
  resetConfig,
  setUserLocale,
  updateToken,
  updateInstanceUrl,
  setUserTimeZone,
  getAvailableTimeZones
} from '@hotwax/oms-api'

export {
  api,
  client,
  getConfig,
  hasError,
  initialise,
  logout,
  resetConfig,
  setUserLocale,
  updateToken,
  updateInstanceUrl,
  setUserTimeZone,
  getAvailableTimeZones
}